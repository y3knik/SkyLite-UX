<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Restrict to app-specific image directory instead of entire external storage -->
    <external-path name="my_images" path="Pictures/SkyLite" />
    <!-- Restrict to app-specific cache subdirectory -->
    <cache-path name="my_cache_images" path="images" />
</paths>