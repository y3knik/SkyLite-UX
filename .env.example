# Database Configuration
# -----------------------
# SQLite (default - simplest option, no external database needed):
# DATABASE_URL="file:/data/skylite.db"
#
# PostgreSQL (for production or multi-user deployments):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/skylight"
#
# If DATABASE_URL is not set, SQLite is used by default at /data/skylite.db

# Prisma Schema Sync (SQLite only)
# --------------------------------
# Set to "true" to allow destructive schema changes during startup.
# WARNING: This may cause data loss if schema changes require dropping columns/tables.
# Only use this if you understand the risks.
# PRISMA_ACCEPT_DATA_LOSS=true

# Google OAuth Configuration (Optional)
# Required for: Google Calendar, Google Photos, Google Tasks integrations
# Get these from Google Cloud Console: https://console.cloud.google.com/
#
# Setup steps:
# 1. Create a project (or select existing)
# 2. Enable APIs: Google Calendar API, Google Tasks API, Google Photos API
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URIs:
#    - http://localhost:3000/api/integrations/google_calendar/callback
#    - http://localhost:3000/api/integrations/google_tasks/callback
#    - http://localhost:3000/api/integrations/google_photos/callback
#    (Replace localhost:3000 with your actual domain/port in production)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Gemini AI API Key (Optional)
# Required for: AI-generated countdown messages
# Get your free API key from: https://aistudio.google.com/app/apikey
# Feature will work without this key using fallback messages
NUXT_GEMINI_API_KEY=
